<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>BuildingBlocks.EventDispatcher</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- Rx 7 discontinued the facade packages (System.Reactive.Linq etc.);
         all operators now ship in the single System.Reactive package. -->
    <PackageReference Include="System.Reactive" />
  </ItemGroup>

</Project>
