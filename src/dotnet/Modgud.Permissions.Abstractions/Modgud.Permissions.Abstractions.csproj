<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>Modgud.Permissions</RootNamespace>
    <AssemblyName>Modgud.Permissions.Abstractions</AssemblyName>
    <Description>Pure permission-evaluation primitives shared by the IdP server-side and external resource-server libs. No persistence / web / DI deps — reuse without transitive bloat.</Description>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Modgud.Tests.Unit" />
  </ItemGroup>

</Project>
