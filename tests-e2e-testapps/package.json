{
  "name": "modgud-testapps-e2e",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Playwright E2E suite for src/dotnet/TestApps and src/test-clients-vue/test-spa-bff. Drives the OAuth client_credentials flow, the BFF cookie+OIDC flow, the YARP proxy and the CSRF guard against a running Modgud instance.",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "typescript": "^5.7.0"
  }
}
